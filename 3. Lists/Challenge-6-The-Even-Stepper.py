"""
Programmatically generate a sequence of numbers starting at two and ending right before twenty, 
but skip every other number (so the gap between numbers is two). Ensure your final output is a 
structured, viewable collection rather than a sequence generator object.

"""

numbers = range(2, 20, 2)

print(list(numbers))